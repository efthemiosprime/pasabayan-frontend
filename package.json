{
  "name": "pasabayan-landing",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sync:snapflow": "cd ../../snap-flow && npm run build && rm -rf ../pasabayan/pasabayan-frontend/public/snapflow && mkdir -p ../pasabayan/pasabayan-frontend/public/snapflow && cp -r dist/* ../pasabayan/pasabayan-frontend/public/snapflow/"
  },
  "devDependencies": {
    "sass": "^1.69.5",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "three": "^0.182.0"
  }
}
